<Alloy>
<!--	<Window class="container" id="videoview">
		<View class = "header">
			<Label id="episode" textid="episode_label"></Label>
			<Label id="episode_number" text="{episode}"></Label>
		</View>
		<ScrollView>
			<View id = "video_content">
				<Label class = "header_text" text="{title}"></Label>
				<VideoPlayer id="video_player" url="{url}" autoplay="true" />
			</View>
			<View id = "video_details">
				<Label id="duration" text="{duration}"></Label>
				<Label id="uploaded" text="{uploaded}"></Label>
				<Label id="description" text="{description}"></Label>
			</View>
		</ScrollView>
	</Window>-->
</Alloy>